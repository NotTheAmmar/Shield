export default function Home() {
    return (
        <div style={{ padding: '2rem', fontFamily: 'sans-serif' }}>
            <h1>Shield Gateway</h1>
            <p>API Gateway is running. Routes are proxied to backend services.</p>
        </div>
    );
}
