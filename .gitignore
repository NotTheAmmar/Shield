node_modules/
.env
.DS_Store
dist/
coverage/
.next/
build/
*.log
.vscode/chrome
# Ignore local DB data
.docker-data/
Documents/
documents/

# Hardhat build output (generated by `npx hardhat compile`)
artifacts/
cache/
typechain-types/

# Blockchain node data (generated by docker-compose.blockchain.yml)
# blockchain/ source files (genesis, keys, keystores) ARE committed intentionally
# Only the runtime chain data is ignored
.docker-data/geth-bootnode/
.docker-data/geth-police/
.docker-data/geth-court/

# Nested github repo clone
github-repo/
tests/results/
